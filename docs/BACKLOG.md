# Backlog — отложенный полишинг

Мелкие, **неблокирующие** правки, которые копим и закрываем **одним скоупом в полишинг-спринт** (вероятно в M5 «Экспорт и полировка»). Сознательно не тащим их поштучно в текущие вехи — это держит фокус и хирургичность правок (CLAUDE.md rule 2/3). Перед M5 пройтись по списку, выбрать актуальное, закрыть пачкой.

Формат: `[ ]` открыто / `[x]` сделано. Поля: область · что · почему отложено · оценка · откуда.

## Открытые

- [ ] **Dev-консоль: sourcemap-шум DuckDB-WASM.** Vite dev-сервер печатает пачку «Sourcemap for …/duckdb-browser-eh.worker.js points to a source file outside its package» (воркеры `@duckdb/duckdb-wasm` ссылаются на исходники `@duckdb/apache-arrow/*.ts` вне пакета). Косметика, **только в `npm run dev`** — в `build`/на Pages этого нет; не наш код. Фикс: `customLogger` в `vite.config.ts`, глушащий ровно эти строки (~10 строк). Оценка: XS. Источник: сессия 2026-06-23 (скрин dev-консоли).

- [ ] **Метка «применить к колонке».** Кнопка в редакторе колонки ре-материализует **всю** таблицу (из текущего конфига), а не только эту колонку. Сейчас метка ок по смыслу «применить правку колонки», но если будет путать — переименовать (напр. «применить»). Оценка: XS. Источник: сессия 2026-06-23.

- [ ] **Неотслеживаемые фикстуры.** В рабочем дереве висят `fixtures/acceptance.sql` и `fixtures/events.csv` (M1-овые приёмочные артефакты). Решить: закоммитить в `fixtures/`, добавить в `.gitignore`, или удалить. `fixtures/dirty.csv` уже закоммичен (M2). Оценка: XS. Источник: сессия 2026-06-23.

- [ ] **Lint-warning `useVirtualizer`.** `react-hooks/incompatible-library` в `src/components/ResultGrid.tsx:17` (TanStack Virtual возвращает не-мемоизируемые функции). Известный, принят, 0 errors. Опционально — точечный `eslint-disable` с комментарием-обоснованием, чтобы lint был чист. Оценка: XS. Источник: M1.

- [ ] **Build chunk-size advisory.** Vite советует «chunks larger than 500 kB» из-за JS-бандла DuckDB-WASM. Ожидаемо (WASM-glue). Опционально — поднять `build.chunkSizeWarningLimit` или code-split, чтобы убрать шум из лога сборки. Оценка: XS, низкая ценность. Источник: M0+.

- [ ] **`WidgetBlockView` — нет сброса в `loading` при смене SQL.** Эффект рерайна не сбрасывает стейт в `{kind:'loading'}` при смене `block.sql`/`client` — старый результат/ошибка висят, пока новый запрос не отработает. В M4 не наступает (sql виджета фиксируется при пине, client один на сессию → эффект срабатывает раз на mount, где начальный стейт уже `loading`). Прямой фикс (синхронный `setState` в начале эффекта) вернул бы `react-hooks/set-state-in-effect`, который имплементер обоснованно обошёл discriminated-union'ом. Если в v1.5 появится правка SQL виджета — сбрасывать в loading через ключ/деривацию, не синхронным setState. Оценка: XS, сейчас теоретический. Источник: M4 slice 1 verifier (Task 6).

- [ ] **Мёртвое поле `Dataset.dirty`.** После перехода M2 на немедленный apply поле `dirty` пишется/чистится в сторе, но UI его больше не читает. Убрать поле + его записи (`stageColumn`/`setColumnConfig`/`setApplied`) и поправить тесты — для чистоты. Оценка: S. Источник: сессия 2026-06-23 (UX-фикс M2).

- [ ] **`TextBlockView` пишет на blur всегда.** `onBlur` зовёт `updateTextBlock` даже если текст не менялся (кликнул в блок и ушёл) → лишний rebuild отчёта + автосейв-запись. Дёшево: `if (draft !== block.markdown) updateTextBlock(...)`. Оценка: XS. Источник: M4 финальный review.

- [ ] **`updateWidgetTitle` без UI-вызова.** Действие в сторе реализовано и юнит-тестировано (часть document-model API по спеке/плану), но заголовок виджета в UI статичен (в отличие от editable подписи). Либо повесить click-to-edit на `.widget-title` (зеркало подписи/`TextBlockView`), либо оставить как API-only с комментарием. Оценка: S. Источник: M4 финальный review.

- [ ] **Пустой «источник(и):» в ошибке виджета.** Если результат закреплён из пустого/scratch-таба без ссылок на таблицы, `datasetNames=[]`, и подсказка при ошибке рисует «источник(и):  — подгрузи…» с пустым списком. Косметика: при `datasetNames.length===0` показывать только текст ошибки. Оценка: XS. Источник: M4 финальный review.

- [ ] **Рейл в Отчёте при активном текст-блоке.** Клик по текст-блоку в Отчёте оставляет `currentSql` на последнем активном табе Исследования → рейл «следит» за устаревшим запросом. По задумке это фоллбэк, но на глаз может выглядеть как баг. Решить по приёмке: либо гасить рейл в Отчёте без активного виджета (`mode==='report' ? null : ...`), либо оставить. Оценка: XS. Источник: M4 финальный review (needs-runtime-check).

### UX / визуал

- [x] **Dropzone — не на всю ширину, в колонку со схемой/источниками** (2026-06-26, M4-полиш). Перенесена из полноширинной верхней полосы в рейл (над «Источники»), компактная (`.rail .dropzone`); сторона подтверждена скрином — левый рейл. Состояние drag-over (`.dropzone.over`) уже было. Доступна в обоих режимах (рейл общий) — заодно удобно для регидрации (дроп из Отчёта).

- [ ] **Редактирование названий табов.** Дать переименовывать таб (double-click по заголовку → inline-инпут; стор-действие `renameTab(id, title)`; Enter/blur сохраняет, Esc отменяет). Сейчас имя = имя датасета или «Запрос N», неизменяемо. Оценка: S. Источник: сессия 2026-06-23.

- [ ] **Табы — визуально ближе к Sublime.** Перерисовать таб-стрип под Sublime-стиль (форма/скос/границы вкладок, выраженный активный таб, close-кнопка на ховере, уплотнить). В основном CSS (`.tab-strip`/`.tab`/`.tab-close`/`.tab-add`). Оценка: S–M. Источник: сессия 2026-06-23.

- [ ] **Показывать `STRING` вместо `VARCHAR` (косметика ярлыка).** В пикере типов и в отображении схемы заменить ярлык `VARCHAR` → `STRING` — привычнее людям из CSV/BigQuery (там строковый тип называется STRING). Чисто презентационно: физический тип и DDL остаются VARCHAR (STRING — его алиас в DuckDB). Релейбл строго на UI-границе (`SchemaColumnEditor` option-label, `col-type` в `Rail`), внутренний `ColType`/SQL не трогать. Оценка: S. Источник: сессия 2026-06-24.

## Сделано

- [x] **Семантика «сброс»/«сбросить» — унифицировано** (2026-06-23). Было: хедерная «сброс» → VARCHAR, а в редакторе колонки «сбросить» → инференс-предложение (сбивало с толку, могло *типизировать* нетипизированную колонку). Стало: оба возвращают к VARCHAR-бейзлайну (`resetColumn` чинён через TDD; per-column «сбросить» = хедерная «сброс», но на одну колонку).
